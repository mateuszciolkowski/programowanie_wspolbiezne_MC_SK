<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:View"
        Title="Ball Board" Height="450" Width="800">

    <Grid>
        <!-- Grupa do układu: w lewym górnym rogu -->
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10">
            <TextBox Text="{Binding AmountOfBalls}" Width="128" Margin="0 0 0 10"/>
            <Button Content="Zastosuj" Command="{Binding ApplyCommand}" Width="100"/>
        </StackPanel>

        <!-- Plansza (gdzie będą się poruszać kulki) - centralnie -->
        <Canvas x:Name="BoardCanvas" Background="LightGray" VerticalAlignment="Center" HorizontalAlignment="Center">
            <ItemsControl ItemsSource="{Binding Balls}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse Width="{Binding Radius}" Height="{Binding Radius}" Fill="Red">
                            <Ellipse.RenderTransform>
                                <TranslateTransform X="{Binding X}" Y="{Binding Y}"/>
                            </Ellipse.RenderTransform>
                        </Ellipse>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Canvas>
    </Grid>
</Window>
